# ReadFlow Studio Agent Guide

ReadFlow Studio 的唯一默认目标是：使用 Obsidian 阅读资料、用户确认的片头话术、书名、正文三段配音和正文 SRT，生成可继续修改的剪映草稿。旧版 Whisper、ASR、HyperFrames 直接渲染文件只在本机保留，不属于公开仓库，也不得自动选择。

## 启动

1. 用 `git rev-parse --show-toplevel` 定位当前仓库，不硬编码旧目录名。
2. 首次使用运行 `npm run init`。主流程只强制要求 Node.js 22+、FFmpeg 和 FFprobe；生成剪映草稿还需要 `uv` 与 CapCut Mate。
3. 本机路径使用 `.readflow.local.json`。旧 `.book-video.local.json` 可自动兼容迁移，但新文档和新输出不得继续使用旧名称。
4. `.readflow.local.json` 的 `schemaVersion: 2` 是本机配置 schema；新项目 `workflow.json` 的 `schemaVersion: 3` 是项目时间线 schema，两者彼此独立，不得因为项目升级而改写本机配置版本。
5. 不要求 Whisper 模型，不自动安装 Whisper，不为主流程运行 ASR。
6. 私密路径、用户书籍、音频、字幕、生成图片和每期输出不得提交。

## 制作关卡

1. 选书并核对书名、作者、版本和封面来源；优先读取 Obsidian 微信读书笔记、划线和个人笔记。
2. 生成一版可直接配音的文案并等待用户确认。文案未确认，不生成分镜图片和剪映草稿。
3. 用户分别提供 `--intro-voice` 片头话术、`--title-voice` 书名、`--voice` 正文三段 MP3，以及从 `00:00:00` 和第一句正文开始的中文 SRT；正文 MP3 和 SRT 均不含书名。英文 SRT 可逐条提供，明确不要英文时使用 `--no-english`。
4. 运行 `workflow:prepare`，生成素材清单、字幕偏移、分镜表和图片提示词。
5. `storyboard.md` 是画面和剪辑的唯一时间真源。用户确认分镜后才生成或放置图片。
6. 图片齐全后先运行 `workflow:draft -- --dry-run`，再生成并安装正式草稿。
7. 新草稿必须使用新名称；不得覆盖用户已有剪映草稿。

如果用户明确要求全自动完成，本期可以跳过分镜确认，但仍必须生成分镜表并完成QA。

## 文案规则

- 短视频开头先给价值和适用人群，迅速说明这本书能解决什么问题。
- 不写生硬说教、空泛道理、机械排比和高高在上的指令。
- 行动必须具体可执行，情绪必须通过空间、动作、声音、光线和身体感受展开。
- 书籍是情绪与观点的支点，不照搬长段划线、整篇书评或受版权保护的正文。
- 片头话术、书名和正文分开交付；书名配音只朗读书名，正文从第一句内容开始，不重复书名。

## 字幕规则

- 用户的中文 SRT 是唯一时间真源，时间从0开始。
- 所有正文字幕统一增加片头话术时长与书名配音时长之和的偏移，与正文音频起点一致。
- 第一条中文字幕必须是第一句正文，不能是目标书名；无法确认正文切换点时停止并要求修正，不能猜测。
- 启用英文字幕时，英文必须与中文逐条对应，文本沿用中文的开始和结束时间。
- 中文字幕始终使用独立轨道；启用英文字幕时再建立英文轨。英文默认字号5，置于中文下方并保留清晰间距。
- 字幕不得早于对应语音；至少检查开头、中段和结尾。

## 分镜和配图规则

- 一分镜一张图，不是一句话一张图；每镜原则上覆盖5～10条正文字幕。
- 分镜数量由文案和字幕数量决定，不固定为7张。
- 使用 `generated/image-prompts.json` 和 `storyboard.md` 记录视觉风格、画面方案、素材状态和修改意见。
- 同一期尽量至少使用4种视觉风格；场景在地点、时间、主体和构图上必须明显不同。
- 禁止真人近景和清晰五官。人物只能是远景、背影或剪影，面积不超过画面10%。
- 至少2镜为纯景物或象征物；内容足够时至少2镜允许极小人物。
- 禁止文字卡片、水印、机甲、战争、血腥和无意义的夸张史诗构图。
- 图片必须按目标画幅生成并保持宽高比，禁止非等比拉伸。

## 三段配音时间线

1. 片头话术配音从0秒开始；画面优先使用快闪图片，其次使用片头 MOV，两者都没有时以全画幅书封兜底。
2. 片头话术结束时显示全画幅书封，书名配音与自动生成的书名字幕同时开始。
3. 书名配音结束后，正文配音、第一条正文字幕和第一张分镜图紧接开始；小封面按模板进入。
4. 书名和作者从书名配音结束后持续到视频结束。
5. 开场画面只按快闪图片、片头 MOV、全画幅书封的优先级选择一种；只有实际选中的素材进入草稿，选中素材存在但损坏时必须报错。
6. BGM、机械音效、水滴音效和正文开头音效可独立叠加：存在时启用，缺失时跳过，存在但损坏时必须报错；实际启用的音效从各自事件点开始。
7. 替换同名可选素材后，新草稿必须读取新文件并复制到草稿资产目录。

## 画幅和排版

- 默认 `3:4`（720×960），另支持 `9:16`（1080×1920）和 `4:3`（960×720）。
- 字体、字号、缩放和位置在 `templates/jianying-draft/layouts.json` 管理。
- 书名、作者、中英文字幕按画布高度比例定位，切换画幅必须重新计算，不得复用固定像素位置。
- 当前纵向锚点：书名约49%、作者约36%、中文约-38%、英文约-50%。
- 任何画幅下都要检查标题与正文、中文与英文没有拥挤或重叠。

## 封面和素材

- 封面优先读取 Obsidian 笔记的 `cover` 字段；找不到时允许用户或 Agent 提供已确认的本地封面文件。
- 全画幅封面必须生成目标视频比例的适配图，原封面本体保持比例，空余区域可用同封面柔化背景补齐；正文小封面使用原始封面并保持比例。
- 片头 MOV、快闪素材、机械音效、水滴音效、正文开头音效和 BGM 都是可选素材；片头话术不属于 `materials` 固定文件，每期从 `--intro-voice` 输入。
- 素材路径必须复制并重写到安装草稿自己的 `assets/`，避免剪映显示“暂无访问权限”。

## 剪映草稿

- 草稿保持视频、图片、片头话术、书名、正文三段配音、实际启用的 BGM 和音效、中文字幕、按需启用的英文字幕、书名、作者、昵称和来源说明为独立可编辑轨道。
- 未提供剪映草稿库路径时必须主动向用户索要，禁止猜测或扫描私人目录。
- 安装前检查目标草稿名是否存在；存在时生成新名称，不覆盖旧草稿。
- 建议用户完全退出并重新打开剪映，以刷新新草稿和素材权限缓存。

## QA

运行 `npm run check`，并对正式草稿确认：

- 画布比例、总时长和音频时长正确。
- 片头话术、书名和正文三段配音边界连续且无重叠。
- 实际启用的开场素材与可选音效事件正确；未启用的素材没有空轨或占位。
- 中文字幕时间与正文配音一致；启用英文字幕时，中英文条数一致、时间一致、位置有间距。
- 每镜5～10条字幕，一镜一图，图片不拉伸。
- 全画幅封面与小封面按实际模板正确显示，图片保持比例。
- 书名和作者从书名结束持续到视频结束。
- 已安装草稿不再引用项目工作目录或 `.tools` 中的临时素材路径。

## 开发兼容规则

- `workflow.json` schema v2 旧项目仍须可读取并按原字段兼容；所有新项目和新生成文档使用 `workflow.json` schema v3 的三段配音结构，不得把旧项目的 `materials.introVoice` 带回新配置。

项目变更完成后更新 `.ai/project.md`，默认使用中文记录。
